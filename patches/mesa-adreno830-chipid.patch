--- a/src/freedreno/common/freedreno_devices.py
+++ b/src/freedreno/common/freedreno_devices.py
@@ -1463,17 +1463,19 @@
             [A6XXRegs.REG_A6XX_UCHE_UNKNOWN_0E12, 0x40000000],
         ],
     ))

-# Totally fake, just to get cffdump to work:
+# Adreno 830 (SM8750 / Snapdragon 8 Elite) — config based on Mesa main.
+# Uses a8xx_gen2 props (= a8xx_gen1 on main) for correct reg_size_vec4=128,
+# cache sizes, and has_salu_int_narrowing_quirk workaround.
 add_gpus([
-        GPUId(chip_id=0x44050000, name="FD830"),
+        GPUId(chip_id=0x440500ff, name="Adreno (TM) 830"),
     ], A6xxGPUInfo(
         CHIP.A8XX,
-        [a7xx_base, a7xx_gen3, a8xx_base],
+        [a7xx_base, a7xx_gen3, a8xx_base, a8xx_gen2],
         num_ccu = 6,
         num_slices = 3,
-        tile_align_w = 64,
+        tile_align_w = 96,
         tile_align_h = 32,
-        tile_max_w = 16384,
-        tile_max_h = 16384,
+        tile_max_w = 16416,
+        tile_max_h = 16384,
         num_vsc_pipes = 32,
         cs_shared_mem_size = 32 * 1024,
         wave_granularity = 2,
         fibers_per_sp = 128 * 2 * 16,
-        magic_regs = dict(
-        ),
-        raw_magic_regs = [
-        ],
+        magic_regs = dict(),
+        raw_magic_regs = a8xx_gen2_raw_magic_regs,
     ))
